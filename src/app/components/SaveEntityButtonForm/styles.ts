import styled from "styled-components";

export const SaveEntityButtonContainer = styled.button`
  width: 120px;
  background: #1e70a0;
  padding: 0.8rem 1rem;
  font-size: 1rem;
  border-radius: 8px;

  cursor: pointer;

  opacity: 70%;
  transition: 1s ease-out;

  &:hover {
    opacity: 100%;
  }
`;
